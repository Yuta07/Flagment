<% if object.errors.any? %>
  <div>
    <p>The form contains </p><%= pluralize(object.errors.count,"error") %>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
